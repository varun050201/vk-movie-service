# Server configuration
server.port=8082

# Spring datasource configuration
spring.datasource.url=jdbc:mysql://localhost:3306/movie  # URL for your MySQL DB
spring.datasource.username=root  # MySQL username
spring.datasource.password=Varun@0502
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA and Hibernate configuration
spring.jpa.hibernate.ddl-auto=update  # Automatically update the schema on app startup
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# SQL initialization
spring.sql.init.mode=always  # Always run schema scripts at startup
